= link_to "Назад", root_path, type: "button", class: "btn btn-default"

%h1= @article.title
%p= @article.content

%h1= @article.titleEn
%p= @article.contentEn

%h1= @article.titleFr
%p= @article.contentFr

%h1= @article.titleCn
%p= @article.contentCn

%h1= @article.titleEs
%p= @article.contentEs

%h1= @article.titleUa
%p= @article.contentUa

.btn-group
	
	= link_to "Редактировать", edit_article_path(@article), class: "btn btn-default"
	= if current_user.admin?
		= link_to "Удалить", article_path(@article), method: :delete,  data: { confirm: "Вы уверены?"}, class: "btn btn-default"